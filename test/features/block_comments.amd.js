define(
  ["rsvp"],
  function(__dependency1__) {
    "use strict";
    var async = __dependency1__.async;
    /* import { foo } from "foo";
    import { bazz } from "bazz";
    import { bar } from "bar";
    import { buzz } from "buzz"; */
  });
